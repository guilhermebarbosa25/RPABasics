RPA inicial de controle navegador

necessário instalar:
pip install rpa
pip install pandas
pip install openpyxl
pip install pyautogui

tarefa:
abre um navegador
baixa uma planilha excel
abre e extrai os dados para um dataframe pandas
insere os dados no formulario e envia para cada linha
